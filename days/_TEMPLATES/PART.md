---
day: NN
part: "S.T"
title: "<what this part teaches, as a phrase — not 'Part 3'>"
ids: [SPK-00]
level: foundation        # foundation | working | production — a folder climbs
prerequisites: ["<the part that must be read first — same folder only — or none>"]
prev: "S.T-<slug>.md"    # relative to this folder; "" on the first part
next: "S.T-<slug>.md"    # relative to this folder; "" on the last part
sources: []              # identifiers this part leans on; drop the key if none
# failure: true          # uncomment on the folder's deliberate-failure part
---

<!--
  Eleven sections, in this order. Three are conditional:
    "The source behind it"    — required exactly when `sources:` above is non-empty
    "Line by line"            — only after a fenced code block; this course has none
    "The source in one demo"  — source documents only; delete it here

  Two headings are in this course's voice: "The rule" (the mechanism) and "In real life" (in
  production). The slots are the same.

  No duration field, no time estimate, no pace — anywhere in this file.
  This part never mentions the other folder. Not once.
  Delete every one of these comments before the part is finished.
-->

## One-line answer

<!-- The rule in one sentence, before anything else. A reader who stops here has still learned
     something true. If you cannot write this sentence, the part is not one idea yet. -->

## The story

<!-- A concrete scene FIRST: a person, a shop counter, a phone call, a misunderstanding. NO JARGON
     AT ALL.

     Four rules:
     1. A scene the reader has plausibly LIVED. A shop. A landlord on the phone. A form at the
        bank. A neighbour at the door. Test: could the reader have been standing in this scene
        themselves?
     2. Simple words. If a twelve-year-old could not follow the first sentence, rewrite it.
     3. Load-bearing. The scene must contain the ACTUAL misunderstanding or decision this part
        teaches — not a pretty image the part then abandons.
     4. One metaphor family per folder per day. Grep the folder's other parts before choosing. -->

## The idea in plain language

<!-- The rule assuming zero prior knowledge. Every term defined on first use — INCLUDING grammar
     words and terms from earlier days, with a link to the part that introduced them and a row in
     docs/GLOSSARY.md. No model lines yet. -->

## Why Vaani needs it

<!-- The concrete later day, or the concrete gate recording or page, that breaks without this.
     Named and linked. Never "this is important" — that sentence carries no information. -->

## The source behind it

<!-- CONDITIONAL: present exactly when `sources:` in the frontmatter is non-empty; delete the
     heading otherwise. An ADDRESS, not an explanation:

       > **<Exact title>** · `<identifier>` · <year>
       > <url>

     One sentence of what it claimed, then a link to the source document that teaches it. -->

## The rule

<!-- How it ACTUALLY works: the pattern, the model lines as blockquotes (never code blocks, never
     more than three in a row), the mouth position or the page layout, a diagram if it is spatial.
     Nothing skipped as "obvious". -->

> <model line one>

> <model line two>

## When it breaks

<!-- The REAL wrong version: what a beginner actually says or writes, written out as it sounds or
     as it looks. What a listener hears instead, or what a reader reads instead. Then the smallest
     fix.

     If you have not heard a beginner make this mistake, say so — do not invent one. -->

> <the wrong version, as it comes out>

## In real life

<!-- NOT OPTIONAL. This is the section that makes the document about a real conversation or a
     real reader rather than a model line, and it is the one that gets dropped.

     Cover: what a fluent speaker or writer does instead of the teaching version · what goes wrong
     under pressure — a fast talker, an angry customer, a formal reader · what a native listener
     notices and forgives, and what they do not · the question a stranger would actually ask. -->

## Check yourself

<!-- One thing to RECORD or WRITE right now, in the learner's own words, about their own life —
     this is Principle 13, and it is the rep. And one question to answer OUT LOUD. Not a quiz —
     a check that can go red, and a sentence you either can or cannot say. -->

- **Record / write:** `TODO(me)` <the prompt — never the answer>
- **Say out loud:** <the question>

---

**Next:** [<title>](<S.T-slug>.md)
