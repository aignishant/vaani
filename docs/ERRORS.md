# ERRORS — append-only

**The most valuable file in this repository.** Days 67 and 68 are written from it and cannot be
written without it.

One row per error you caught **in your own recording**. Not errors someone told you about —
errors you heard. Log the same error every time it happens; the repetition is the signal.

| Date | Day | What I said | What it should be | Type |
| --- | --- | --- | --- | --- |

Types: `sound` · `stress` · `grammar` · `word` · `freeze` · `filler` · `speed`
